<%= @organization.name %> - <%= @report_type.humanize %> Report
<%= "=" * (@organization.name.length + @report_type.humanize.length + 12) %>

Hello <%= @user.first_name || @user.email %>,

REPORT SUMMARY
--------------
Report Type: <%= @report_type.humanize %>
Generated: <%= Time.current.strftime('%B %d, %Y at %I:%M %p %Z') %>

<% if @report_data.is_a?(Hash) %>
<% @report_data.each do |key, value| %>
<%= key.to_s.humanize %>: <% if value.is_a?(Hash) %>
<%= value.map { |k, v| "#{k.to_s.humanize}: #{v}" }.join(', ') %>
<% else %>
<%= value %>
<% end %>
<% end %>
<% end %>

REPORT ATTACHMENTS
------------------
This email includes your report data in multiple formats:

* PDF Format: Professional formatted report suitable for sharing and printing
* CSV Format: Raw data that can be imported into spreadsheet applications

All attached files have been securely validated and are safe to download.

---
This multi-format report was automatically generated by the Data Refinery Platform.
If you have any questions about the report data or attachments, please contact your system administrator.

<%= @organization.name %> | Data Refinery Platform
You received this email because you are subscribed to <%= @report_type.humanize.downcase %> reports from this organization.